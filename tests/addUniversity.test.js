/*
This unit test validates that a university is successeffully added



*/

import * as AWSMock from 'aws-sdk-mock';
import * as AWS from 'aws-sdk';

describe('Add a univeristy to the table ', () => {

test('Successfully put the data into dynamoDB', async () => {

})

test('does not put the data into dynamoDB', async () => {

})


})